# 测试手册 - 本轮 Task Template 持久化与编辑验证

**测试日期**: 2026-05-09  
**本次测试范围**: 仅测试本轮刚修改的 Task Template 功能  
**预计时间**: 10-15 分钟

---

## 说明

本手册只保留**本次需要测试**的内容，聚焦以下能力：
- 默认 3 个模板是否已初始化到数据库
- 模板列表是否从数据库正确展示
- 默认模板初始状态是否正确
- 点击 `Edit` 后是否可以修改并保存
- 保存后 UI 是否即时刷新
- 重启应用后数据是否仍然保留
- `Save task as template` 是否写入数据库

---

## 准备工作

1. 启动开发服务器：`npm run dev`
2. 启动桌面端（按当前项目的 Tauri 开发方式启动）
3. 打开界面后进入 Task / Template 相关页面
4. 建议打开开发者控制台，方便查看日志

---

## 本次测试项

### A1: 默认 3 个模板初始化并显示

**当前状态**: 未开始测试

**操作步骤**:
1. 启动应用
2. 进入模板选择/模板列表页面
3. 观察系统默认模板是否显示

**预期效果**:
- 能看到默认 3 个系统模板
- 模板可以正常打开详情或进入编辑
- 模板列表不是空的

---

### A2: 默认模板初始状态正确

**当前状态**: 未开始测试

**操作步骤**:
1. 在模板列表中观察默认 3 个模板卡片右上角状态
2. 不做任何编辑，直接检查初始显示

**预期效果**:
- 默认模板不应显示绿色 `✅`
- 因为默认 role 的 `provider` 初始为空，所以应显示“未配置 / 可编辑”一类的非完成态
- 不应误判为已完成模板

---

### A3: 点击 Edit 后可以修改 provider 并保存

**当前状态**: 未开始测试

**操作步骤**:
1. 在任意默认模板卡片上点击 `Edit`
2. 修改一个 role 的 `provider`
3. 如界面允许，也可顺手修改名称或描述
4. 点击保存
5. 关闭编辑界面

**预期效果**:
- Edit 页面不是只读
- 可以修改 `provider`
- 点击保存后没有报错
- 返回列表后能看到状态或内容已更新

---

### A4: Edit 保存后列表和详情即时刷新

**当前状态**: 未开始测试

**操作步骤**:
1. 选中一个默认模板
2. 点击 `Edit`
3. 修改 `provider` 或模板字段并保存
4. 保存后不刷新页面，直接观察当前界面

**预期效果**:
- 模板列表即时刷新
- 当前选中的模板详情即时刷新
- 卡片右上角状态即时刷新
- 不需要手动关闭页面或重进才能看到新值

---

### A5: 重开模板后数据仍然存在

**当前状态**: 未开始测试

**操作步骤**:
1. 编辑并保存某个默认模板
2. 关闭当前编辑界面
3. 重新打开同一个模板详情或编辑页

**预期效果**:
- 刚才设置的 `provider` 仍然存在
- 模板名称/描述等修改项仍然存在（如果有修改）
- 说明数据已写入数据库，而不是只存在前端内存

---

### A6: 重启应用后模板修改仍保留

**当前状态**: 未开始测试

**操作步骤**:
1. 先完成一次模板编辑并保存
2. 完全退出应用
3. 重新启动应用
4. 再次进入模板列表并打开刚才修改的模板

**预期效果**:
- 之前保存的 `provider` 仍然存在
- 模板状态与上次保存后一致
- 说明模板修改已成功持久化到 SQLite

---

### A7: Save task as template 写入数据库

**当前状态**: 未开始测试

**操作步骤**:
1. 打开一个已有 Task
2. 点击 `保存为模板`
3. 输入模板名称和描述
4. 保存后回到模板列表

**预期效果**:
- 能看到新建的 user template
- 新模板可打开详情
- 重新进入后仍然存在

---

## 测试结果记录

| 功能 | 状态 | 备注 |
|------|------|------|
| A1 默认模板初始化显示 | ⬜ 未测试 | |
| A2 默认模板初始状态 | ⬜ 未测试 | |
| A3 Edit 修改 provider 并保存 | ⬜ 未测试 | |
| A4 保存后 UI 即时刷新 | ⬜ 未测试 | |
| A5 重开模板后数据保留 | ⬜ 未测试 | |
| A6 重启应用后数据保留 | ⬜ 未测试 | |
| A7 Save task as template | ⬜ 未测试 | |

**状态说明**:
- 通过
- 失败
- 部分通过
- 未测试

---

## 常见问题

### Q1: 模板列表为空
- 检查应用是否正确启动数据库初始化
- 检查是否成功插入默认 3 个模板
- 检查前端是否成功调用模板查询接口

### Q2: Edit 保存后看不到变化
- 检查保存是否成功
- 检查列表与详情是否有刷新
- 关闭后重新打开同一模板再次确认

### Q3: 重启后模板修改丢失
- 说明可能还没有真正写入 SQLite
- 需要重点检查模板更新接口与数据库 update 逻辑

---

**请只按以上 A1-A7 进行本轮测试。**
